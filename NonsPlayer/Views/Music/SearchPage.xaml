<Page
    x:Class="NonsPlayer.Views.Pages.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:NonsPlayer.Components.Views"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer ViewChanged="{x:Bind ViewModel.OnScrollViewerViewChanged}">
            <Grid Margin="{StaticResource PlaylistDetailMargin}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Grid VerticalAlignment="Top">
                    <views:BestMusicCard HorizontalAlignment="Left" />
                    <views:BestArtistCard HorizontalAlignment="Right" />
                </Grid>
                <views:MusicListBar Grid.Row="1" MusicItems="{x:Bind ViewModel.MusicItems, Mode=OneWay}" />
            </Grid>
        </ScrollViewer>
    </Grid>
</Page>