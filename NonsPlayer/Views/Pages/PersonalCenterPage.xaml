<Page
    x:Class="NonsPlayer.Views.Pages.PersonalCenterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="{x:Bind ViewModel.PersonalCenterPage_OnLoaded}">

    <Grid>
        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Name="Face" Height="200" Width="200" CornerRadius="180"
                    Background="{x:Bind ViewModel.Account.Face, Mode=OneWay}" Margin="0,0,0,20" />
            <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
                <TextBlock Text="早上好!"
                           FontWeight="Black" FontSize="15" FontFamily="HarmonyOS Sans SC" />
                <TextBlock Text="{x:Bind ViewModel.Account.Name, Mode=OneWay}"
                           FontWeight="Black" FontSize="15" FontFamily="HarmonyOS Sans SC" />
            </StackPanel>
        </Grid>
    </Grid>
</Page>