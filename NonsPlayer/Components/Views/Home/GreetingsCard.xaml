<UserControl
    x:Class="NonsPlayer.Components.Views.GreetingsCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:winUi="using:CommunityToolkit.WinUI"
    mc:Ignorable="d">
    <Border Background="#F5F3F2"
            CornerRadius="20"
            Width="287" Height="99">
        <Grid Grid.Row="0" Margin="20">
            <TextBlock x:Uid="GreetingsCard_Night"
                       FontWeight="Bold" FontSize="30"
                       Foreground="{StaticResource CommonTextColor}"
                       VerticalAlignment="Center" HorizontalAlignment="Left" />
            <TextBlock Text="{x:Bind ViewModel.TimeString, Mode=OneWay}"
                       FontWeight="Bold" FontSize="48" Foreground="#3662EC"
                       VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,10,0" />
        </Grid>
    </Border>
</UserControl>